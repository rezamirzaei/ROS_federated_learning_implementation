"""Package marker for launch_testing-based ROS2 tests.

These run under `colcon test` and require the full ROS2 toolchain plus
the custom `fl_robots_interfaces` message package. Excluded from the
standalone pytest suite.
"""
